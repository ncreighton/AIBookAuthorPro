<UserControl
    x:Class="AIBookAuthorPro.UI.Views.LocationListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:converters="clr-namespace:AIBookAuthorPro.UI.Converters"
    xmlns:vm="clr-namespace:AIBookAuthorPro.UI.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vm:LocationListViewModel}"
    d:DesignHeight="700"
    d:DesignWidth="1000">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
        <converters:ZeroToVisibilityConverter x:Key="ZeroToVisibility" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:Card
            Grid.Row="0"
            Margin="16,16,16,8"
            Padding="24"
            materialDesign:ElevationAssist.Elevation="Dp2">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button
                    Style="{StaticResource MaterialDesignIconButton}"
                    Command="{Binding GoBackCommand}"
                    ToolTip="Back to Dashboard"
                    Margin="0,0,16,0">
                    <materialDesign:PackIcon Kind="ArrowLeft" Width="24" Height="24" />
                </Button>

                <StackPanel Grid.Column="1">
                    <TextBlock
                        Text="Locations"
                        Style="{StaticResource MaterialDesignHeadline5TextBlock}" />
                    <TextBlock
                        Text="{Binding StatusMessage}"
                        Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                        Foreground="{DynamicResource MaterialDesignBodyLight}" />
                </StackPanel>

                <Button
                    Grid.Column="2"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding CreateNewLocationCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Plus" Margin="0,0,8,0" />
                        <TextBlock Text="New Location" />
                    </StackPanel>
                </Button>
            </Grid>
        </materialDesign:Card>

        <!-- Search -->
        <materialDesign:Card
            Grid.Row="1"
            Margin="16,0,16,8"
            Padding="16">

            <TextBox
                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                materialDesign:HintAssist.Hint="Search locations..."
                materialDesign:TextFieldAssist.HasClearButton="True" />
        </materialDesign:Card>

        <!-- Location Grid -->
        <ScrollViewer
            Grid.Row="2"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollBarVisibility="Disabled"
            Padding="8,0">

            <ItemsControl ItemsSource="{Binding FilteredLocations}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card
                            Width="300"
                            Margin="8"
                            Cursor="Hand"
                            MouseLeftButtonUp="OnLocationCardClick">

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="100" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <!-- Location Icon Area -->
                                <Border
                                    Background="{DynamicResource PrimaryHueLightBrush}"
                                    CornerRadius="4,4,0,0">

                                    <Grid>
                                        <!-- Icon -->
                                        <materialDesign:PackIcon
                                            Kind="MapMarker"
                                            Width="48"
                                            Height="48"
                                            Foreground="{DynamicResource PrimaryHueMidBrush}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center" />

                                        <!-- Type Badge -->
                                        <materialDesign:Chip
                                            Content="{Binding Type}"
                                            Margin="8"
                                            VerticalAlignment="Top"
                                            HorizontalAlignment="Right"
                                            FontSize="10" />
                                    </Grid>
                                </Border>

                                <!-- Info Area -->
                                <StackPanel Grid.Row="1" Margin="16">
                                    <TextBlock
                                        Text="{Binding Name}"
                                        Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                        TextTrimming="CharacterEllipsis" />

                                    <TextBlock
                                        Text="{Binding Description}"
                                        Style="{StaticResource MaterialDesignBody2TextBlock}"
                                        TextTrimming="CharacterEllipsis"
                                        MaxHeight="40"
                                        Margin="0,8,0,0" />

                                    <!-- Actions -->
                                    <StackPanel
                                        Orientation="Horizontal"
                                        HorizontalAlignment="Right"
                                        Margin="0,12,0,0">

                                        <Button
                                            Style="{StaticResource MaterialDesignIconButton}"
                                            Command="{Binding DataContext.EditLocationCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Edit">
                                            <materialDesign:PackIcon Kind="Pencil" />
                                        </Button>

                                        <Button
                                            Style="{StaticResource MaterialDesignIconButton}"
                                            Command="{Binding DataContext.DuplicateLocationCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Duplicate">
                                            <materialDesign:PackIcon Kind="ContentCopy" />
                                        </Button>

                                        <Button
                                            Style="{StaticResource MaterialDesignIconButton}"
                                            Command="{Binding DataContext.DeleteLocationCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Foreground="{DynamicResource MaterialDesignValidationErrorBrush}"
                                            ToolTip="Delete">
                                            <materialDesign:PackIcon Kind="Delete" />
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty State -->
        <StackPanel
            Grid.Row="2"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Visibility="{Binding FilteredLocations.Count, Converter={StaticResource ZeroToVisibility}}">

            <materialDesign:PackIcon
                Kind="MapMarkerMultiple"
                Width="64"
                Height="64"
                Foreground="{DynamicResource MaterialDesignBodyLight}"
                HorizontalAlignment="Center" />

            <TextBlock
                Text="No locations found"
                Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                Foreground="{DynamicResource MaterialDesignBodyLight}"
                HorizontalAlignment="Center"
                Margin="0,16,0,0" />

            <Button
                Style="{StaticResource MaterialDesignOutlinedButton}"
                Content="Create First Location"
                Command="{Binding CreateNewLocationCommand}"
                HorizontalAlignment="Center"
                Margin="0,16,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
